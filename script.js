const TicTacToe = () => {
    const GAMEBOARD = (() => {
        let gameBoard = []
        return gameBoard
    })();

    const PLAYER = (name, shape) => {
        let score = 0;
        let getName = () => name;
        let getShape = () => shape;
    }
}